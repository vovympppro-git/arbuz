name: Test Self-Hosted Runner
<<<<<<< HEAD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Show server info
        run: |
          echo "=== Server Information ==="
          hostname
          whoami
          pwd
          date
          echo "=== Disk Space ==="
          df -h
          echo "=== Memory ==="
          free -h
      
      - name: Create test file
        run: |
          echo "Self-hosted runner works!" > test.txt
          cat test.txt
=======
 
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # позволяет запустить вручную
 
jobs:
  test:
    runs-on: self-hosted  # использует ваш сервер
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Show system info
        run: |
          echo "=== System Information ==="
          hostname
          whoami
          pwd
          uname -a
          df -h
          free -h
      - name: Create test file
        run: |
          echo "Test from self-hosted runner" > test.txt
          cat test.txt
      - name: List running processes
        run: ps aux | head -10
>>>>>>> 8f08d50 (1)
